{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsMarbleNew",
  "title": "Marble.js New Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the new project.",
      "type": "string",
      "format": "html-selector",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for this project?"
    },
    "directory": {
      "type": "string",
      "description": "The directory name to create the project in."
    }
  },
  "required": ["name"]
}
